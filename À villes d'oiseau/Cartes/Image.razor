<img src="@image.Chemin" style="object-position: @Position" />

@code {
    [Parameter]
    public A_villes_d_oiseau.Villes.Image image { get; set; } = default!;

    string Position => (image.DecalageHorizontal == 0 ? "left" : "right");
}
